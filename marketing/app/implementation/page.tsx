import type { Metadata } from 'next';
import { MarketingPage } from '../MarketingPage';
import { metadata } from '../../lib/seo';
export const generateMetadata = (): Metadata => metadata('Implementation and Onboarding | Pace', 'See how Pace can be introduced in practical stages across a hospitality venue.', '/implementation');
const phases=[['01','Understand the venue','Map your tables, service periods, opening and kitchen hours, menus, team roles and current guest journey.'],['02','Configure the foundation','Set up the booking diary, table capacities, menu catalogue, availability rules and initial loyalty structure.'],['03','Prepare the team','Walk through the workflows staff will use during a real shift, from reviewing bookings to maintaining menu information.'],['04','Introduce the guest experience','Shape the app content, venue information, booking journey and rewards around what you want customers to do next.'],['05','Review and improve','Use the first phase of operation to identify friction, refine configuration and plan the next useful capability.']];
export default function ImplementationPage(){return <MarketingPage eyebrow="IMPLEMENTATION AND ONBOARDING" title={<>A rollout that respects the <em>working day.</em></>} intro="Good implementation is not about turning on every feature at once. It is about giving the team a useful first version, building confidence and improving from there."><div className="phase-list">{phases.map(([number,title,copy])=><article key={number}><span>{number}</span><div><h2>{title}</h2><p>{copy}</p></div></article>)}</div><div className="implementation-note"><h2>Start focused. Expand with confidence.</h2><p>You can begin with the workflow creating the most friction, then add menus, rewards and the guest app as your operation is ready.</p></div></MarketingPage>}
