/** Static counterparts for contexts that cannot resolve CSS custom properties
 * (PWA metadata and the SVG barcode renderer). */
export const designTokens = {
  brand: '#cf122d',
  appSurface: '#f7f7f7',
  barcodeSurface: '#ffffff',
  barcodeInk: '#111111',
} as const;
